# M0: Foundation

> Historical implementation reference

M0 established the generated Xcode project, local Swift package structure, and the module-boundary rule that keeps `FleetUI` independent of `FleetNetworking`.

For the current architecture, see [`architecture.md`](architecture.md). Detailed internal task and review evidence is intentionally omitted.
